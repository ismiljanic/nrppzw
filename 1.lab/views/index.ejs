<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="/styles/styles.css" rel="stylesheet">
</head>

<body class="bg-gradient-to-r from-blue-200 to-blue-400 font-sans flex flex-col justify-between h-screen">
    <div class="flex flex-col items-center justify-center flex-grow">
        <h1 class="text-8xl font-bold text-center mt-10 drop-shadow-lg">Welcome</h1>

        <div class="text-center mt-5">
            <div class="text-center mt-5">
                <% if (isAuthenticated) { %>
                    <p class="text-lg text-gray-800">
                        You are logged in.
                    </p>
                    <a href="/logout" class="btn mt-2 transition-transform hover:scale-105">Logout</a>
                    <% } else { %>
                        <a href="/login" class="btn transition-transform hover:scale-105 btn-custom-margin">Login</a>
                        <% } %>
            </div>


        </div>

        <div class="container">
            <div class="ticket-count text-center p-6 mt-10 bg-white rounded-lg shadow-xl transform transition-all">
                <strong class="text-3xl md:text-4xl">Total Tickets:</strong>
                <span class="text-5xl font-bold block mt-2 text-blue-600">
                    <%= ticketCount %>
                </span>
            </div>
        </div>
        
    </div>

    <div class="text-center mb-10">
        <h3 class="text-lg font-semibold text-gray-800">Create Ticket</h3>
        <p class="text-gray-600 mt-2">Want to create a new ticket?</p>
        <a href="/create-ticket" class="btn mt-2 transition-transform hover:scale-105">Go to Create Ticket</a>
    </div>
</body>

</html>
